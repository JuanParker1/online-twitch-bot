// OTB_UserPointsPage React Component
//
// Generated by IC STRATEGY
//
// WARNING: Do not change this code; it will be overwritten by the next generation run!
//          Change the code only in the Visual Paradigm Project.

var DomainAPI = require('../domain-entity/DomainAPI');
import React from 'react';
import * as _ from 'lodash';
import { withRouter } from 'react-router-dom';
import * as firebase from 'firebase';
import { FirebaseManager } from '../../../js/4-infrastructure/databaseManagers/FirebaseManager';
import OTB_PointsSection from './OTB_PointsSection';

class OTB_UserPointsPage extends React.Component {
    constructor(props) {
        super(props);
        this.state = {
            user: null
        };
    };
    static defaultProps = {
        application: null
    };
    render() {
        var self = this;
        if (true) {
            return(
                <div className={"page user-points-page " + 'flex-grid page-content padding40'} style={self.props.style || {"height": "100%"}} >
                    { (self.state.user) ? <OTB_PointsSection user={self.state.user} /> : <span /> }
                </div>
            );
        }
        else {
            return <span />;
        };
    };
    componentDidMount() {
        DomainAPI.OTB_UserConnect(this, 'user');
        var self = this;
        
                var username = self.props.match.params.username.toLowerCase();
                var ref = firebase.database().ref("OTB_User/");
        
                ref.once("value", function (snapshot) {
                    var data = snapshot.val();
                    _.each(data, function(obj, key){
                        var attributes = obj['attributes'];
                        if(attributes['displayName'].toLowerCase() == username){
                            var id = attributes['id'];
                            DomainAPI.OTB_UserLoadItem(id);
                        }
                    });
                });
    };
};

export default withRouter(OTB_UserPointsPage);
