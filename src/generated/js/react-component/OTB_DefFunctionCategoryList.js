// OTB_DefFunctionCategoryList React Component
//
// Generated by IC STRATEGY
//
// WARNING: Do not change this code; it will be overwritten by the next generation run!
//          Change the code only in the Visual Paradigm Project.

var DomainAPI = require('../domain-entity/DomainAPI');
import React from 'react';
import * as _ from 'lodash';
import OTB_ReactDefaultFunctionCategory from './OTB_ReactDefaultFunctionCategory';
import OTB_AddDefaultFunctionCategory from './OTB_AddDefaultFunctionCategory';

class OTB_DefFunctionCategoryList extends React.Component {
    constructor(props) {
        super(props);
    };
    static defaultProps = {
        application: null,
        role: 'dropdown',
        user: null
    };
    render() {
        var self = this;
        if (true) {
            return(
                <ul className={"def-function-category-list " + 'd-menu toggle-dropdown-menu'} style={self.props.style || {}} data-role={this.props.role} >
                    { (self.props.user && self.props.user.hasRole('administrator')) ? <OTB_AddDefaultFunctionCategory application={self.props.application} /> : <span /> }
                    { ((true) && (self.props.application && self.props.application.get('appFunctionCategories') && self.props.application.get('appFunctionCategories'))) ? self.props.application.get('appFunctionCategories').map(function(item, index) {
                        return (<OTB_ReactDefaultFunctionCategory defaultFunctionCategory={item} application={self.props.application} index={index} user={self.props.user} key={item.id || index} />);
                    }) : '' }
                </ul>
            );
        }
        else {
            return <span />;
        };
    };
};

export default OTB_DefFunctionCategoryList;
