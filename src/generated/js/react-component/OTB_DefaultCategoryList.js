// OTB_DefaultCategoryList React Component
//
// Generated by IC STRATEGY
//
// WARNING: Do not change this code; it will be overwritten by the next generation run!
//          Change the code only in the Visual Paradigm Project.

var DomainAPI = require('../domain-entity/DomainAPI');
import React from 'react';
import * as _ from 'lodash';
import OTB_AddDefaultCategory from './OTB_AddDefaultCategory';
import OTB_DefaultCategory from './OTB_DefaultCategory';

class OTB_DefaultCategoryList extends React.Component {
    constructor(props) {
        super(props);
    };
    static defaultProps = {
        application: null,
        role: 'dropdown',
        user: null
    };
    render() {
        var self = this;
        if (true) {
            return(
                <ul className={"default-category-list " + 'd-menu toggle-dropdown-menu'} style={self.props.style || {}} data-role={this.props.role} >
                    { (self.props.user && self.props.user.hasRole('administrator')) ? <OTB_AddDefaultCategory application={self.props.application} /> : <span /> }
                    { ((true) && (self.props.application && self.props.application.get('appCommandCategories') && self.props.application.get('appCommandCategories'))) ? self.props.application.get('appCommandCategories').map(function(item, index) {
                        return (<OTB_DefaultCategory application={self.props.application} defaultCategory={item} index={index} user={self.props.user} key={item.id || index} />);
                    }) : '' }
                </ul>
            );
        }
        else {
            return <span />;
        };
    };
};

export default OTB_DefaultCategoryList;
