// OTB_UserLeftMenu React Component
//
// Generated by IC STRATEGY
//
// WARNING: Do not change this code; it will be overwritten by the next generation run!
//          Change the code only in the Visual Paradigm Project.

import React from 'react';
import * as _ from 'lodash';
var DomainAPI = require('../domain-entity/DomainAPI');
import { withRouter } from 'react-router-dom';
import OTB_HeaderMenuButton from './OTB_HeaderMenuButton';

class OTB_UserLeftMenu extends React.Component {
   constructor(props) {
       super(props);
   };
   static defaultProps = {
           location: '/',
           user: null
   };
   render() {
       var self = this;
       if (true){ 
           return(
               <ul className={"user-left-menu " + 'app-bar-menu'} style={self.props.style ||{}} >
                  {(self.props.user) ? <OTB_HeaderMenuButton link={'user/' + self.props.user.get('displayName').toLowerCase() + '/commands'} title={'Commands'} user={self.props.user} /> : <span />}
                  {(self.props.user && self.props.user.get('ownPointRanks').get) ? <OTB_HeaderMenuButton link={'user/' + self.props.user.get('displayName').toLowerCase() + '/points'} title={'Points'} user={self.props.user} /> : <span />}
                  {(self.props.user && self.props.user.get('ownLevelRanks').get) ? <OTB_HeaderMenuButton link={'user/' + self.props.user.get('displayName').toLowerCase() + '/levels'} title={'Levels'} user={self.props.user} /> : <span />}
               </ul>
           );
       } else {
           return (<span />);
       }
   };
   componentDidMount() {
   };
   componentWillUnmount() {
   };
}

export default withRouter(OTB_UserLeftMenu);
