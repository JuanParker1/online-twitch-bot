// OTB_Header React Component
//
// Generated by IC STRATEGY
//
// WARNING: Do not change this code; it will be overwritten by the next generation run!
//          Change the code only in the Visual Paradigm Project.

import React from 'react';
import * as _ from 'lodash';
var DomainAPI = require('../domain-entity/DomainAPI');
import { withRouter } from 'react-router-dom';
import OTB_Logo from './OTB_Logo';
import OTB_RightMenu from './OTB_RightMenu';
import OTB_UserLeftMenu from './OTB_UserLeftMenu';

class OTB_Header extends React.Component {
   constructor(props) {
       super(props);
   };
   static defaultProps = {
           location: '/',
           user: null,
           application: null
   };
   render() {
       var self = this;
       if (true){ 
           return(
               <div className={"header " + 'app-bar fixed-top navy'} style={self.props.style ||{}} >
                  <OTB_Logo application={self.props.application} />
                  {(self.props.location.pathname.indexOf('user') <= -1) ? <OTB_RightMenu location={self.props.location} user={self.props.user} /> : <span />}
                  {(self.props.location.pathname.indexOf('user') > -1) ? <OTB_UserLeftMenu location={self.props.location} user={self.props.user} /> : <span />}
               </div>
           );
       } else {
           return (<span />);
       }
   };
   componentDidMount() {
   };
   componentWillUnmount() {
   };
}

export default withRouter(OTB_Header);
