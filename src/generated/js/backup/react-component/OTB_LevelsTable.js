// OTB_LevelsTable React Component
//
// Generated by IC STRATEGY
//
// WARNING: Do not change this code; it will be overwritten by the next generation run!
//          Change the code only in the Visual Paradigm Project.

import React from 'react';
import * as _ from 'lodash';
var DomainAPI = require('../domain-entity/DomainAPI');
import OTB_Table from './OTB_Table';
import OTB_LevelTableInfo from './OTB_LevelTableInfo';

class OTB_LevelsTable extends React.Component {
   constructor(props) {
       super(props);
   };
   static defaultProps = {
           user: null,
           levelRanks: null
   };
   render() {
       var self = this;
       if (true){ 
           return(
               <OTB_Table className="o-t-b_-levels-table"  titles={['Rank', 'User Name', 'Experience', 'Level', 'Next Level Experience']}>
{                  self.sortLevels() && self.sortLevels() ? self.sortLevels().map(function(item, index) {
                      return(<OTB_LevelTableInfo levelParticipator={item} index={index}  key={item.id || index} />);
                  }) : ''}
               </OTB_Table>
           );
       } else {
           return (<span />);
       }
   };
   sortLevels = () => {
       var self = this;
var levelParts = self.props.levelRanks.get('levelParticipators');
var sortArray = _.sortBy(levelParts, function(obj){ return obj.get('experience'); });
return sortArray.reverse();
   };
   componentDidMount() {
   };
   componentWillUnmount() {
   };
}

export default OTB_LevelsTable;
