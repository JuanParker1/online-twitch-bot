// OTB_ButtonCommand React Component
//
// Generated by IC STRATEGY
//
// WARNING: Do not change this code; it will be overwritten by the next generation run!
//          Change the code only in the Visual Paradigm Project.

import React from 'react';
import * as _ from 'lodash';
var DomainAPI = require('../domain-entity/DomainAPI');

class OTB_ButtonCommand extends React.Component {
   constructor(props) {
       super(props);
   };
   static defaultProps = {
           title: '',
           onClick: function(){},
           style: '',
           iconClass: ''
   };
   render() {
       var self = this;
var hasIcon = false;
if(self.props.iconClass != '')
    hasIcon = true;
if(hasIcon){
    return(
        <a onClick={self.onClick} className={self.props.style + " has-icon"}><span className={self.props.iconClass}></span>{self.props.title}</a>
    );
}
else{
    return(
        <a onClick={self.onClick} className={self.props.style}>{self.props.title}</a>
    );
}
   };
   onClick = (event) => {
       var self = this;
event.stopPropagation();
event.preventDefault();
if (self.props.onClick) {
    self.props.onClick();
}
   };
   componentDidMount() {
   };
   componentWillUnmount() {
   };
}

export default OTB_ButtonCommand;
