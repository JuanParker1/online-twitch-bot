// OTB_Plugin Controllers
// 
// Generated by IC STRATEGY
//
// WARNING: Do not change this code; it will be overwritten by the next generation run!
//          Change the code only in the Visual Paradigm Project.

import * as Form from '../form/OTB_Plugin';
import {OTB_Plugin} from '../domain-entity/OTB_Plugin';
var DomainAPI = require('../domain-entity/DomainAPI');

var CreateController = function(contextObject) {
    var form = new Form.Create(contextObject).open();
    $.when(form).done(function(formData) {
        if (formData) {
            switch(formData.action) {
                case 'save':
                    formData.values = formData.values || {};
                    if (contextObject['variablePlugins']) {
                        formData.values['variablePlugins'] = contextObject['variablePlugins'];
                    }
                    if (contextObject['functionPlugins']) {
                        formData.values['functionPlugins'] = contextObject['functionPlugins'];
                    }
                    if (contextObject['commandPlugins']) {
                        formData.values['commandPlugins'] = contextObject['commandPlugins'];
                    }
                    if (contextObject['ownPlugins']) {
                        formData.values['ownPlugins'] = contextObject['ownPlugins'];
                    }
                    if (contextObject['usedPlugins']) {
                        formData.values['usedPlugins'] = contextObject['usedPlugins'];
                    }
                    if (contextObject['repositoryPlugins']) {
                        formData.values['repositoryPlugins'] = contextObject['repositoryPlugins'];
                    }                    
DomainAPI.OTB_PluginCreate(formData.values, contextObject.user);
                    break;
            }
        }
    });
};
var UpdateController = function(contextObject) {
    function openUpdateForm(loadedEntity, user) {
        var form = new Form.Update(contextObject).open(loadedEntity.getValues());
        $.when(form).done(function(formData) {
            if (formData) {
                switch(formData.action) {
                    case 'save':
                        loadedEntity.setValues(formData.values);
                        DomainAPI.OTB_PluginUpdate(loadedEntity, formData.values, user);
                        break;
                    case 'delete':
                        DomainAPI.OTB_PluginDelete(loadedEntity, user);
                        break;
                }
            }
        });
    }
    var entity = contextObject.entity || contextObject;
    if (entity.loadStatus === 'proxy') {
        entity.loadInstance(OTB_Plugin, entity.id, function(loadedEntity) {
            openUpdateForm(loadedEntity, contextObject.user);
        }, contextObject.user);
    } else {
        openUpdateForm(entity, contextObject.user);
    }
};
module.exports = {
    Create: CreateController,
    Update: UpdateController}

